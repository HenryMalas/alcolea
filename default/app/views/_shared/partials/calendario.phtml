<div class="my-5" id="calendario">
  <div class="section-title"><h2>Calendario</h2><span class="subtitle">Fechas con actividad resaltadas y enlazadas a secciones</span></div>
  <div class="calendar">
    <div class="cal" aria-live="polite">
      <div class="cal-head">
        <button id="prevMonth" aria-label="Mes anterior">◀</button>
        <div class="title" id="calTitle">Mes Año</div>
        <button id="nextMonth" aria-label="Mes siguiente">▶</button>
      </div>
      <div class="cal-grid" id="calGrid">
        <div class="dow">L</div><div class="dow">M</div><div class="dow">X</div><div class="dow">J</div><div class="dow">V</div><div class="dow">S</div><div class="dow">D</div>
      </div>
    </div>
    <aside class="event-panel">
      <h3>Próximas actividades</h3>
      <div id="eventList">
        <?php foreach ($events as $ev): ?>
          <div class="event">
            <strong><?php echo $ev->title ?></strong>
            <div class="k"><?php echo $ev->category ?> · <?php echo date('d/m/Y', strtotime($ev->start_date)) ?></div>
            <?php if ($ev->place): ?><div><?php echo $ev->place ?></div><?php endif; ?>
            <?php if ($ev->section_id): ?><a class="btn" style="margin-top:.4rem" href="/sections/show/<?php echo (new Sections)->find_first($ev->section_id)->slug ?>">Ver sección</a><?php endif; ?>
          </div>
        <?php endforeach ?>
      </div>
      <p class="subtitle">*Datos de ejemplo. Sustituir por reales.</p>
    </aside>
  </div>
</div>
