<div class="galeria">
    <?php foreach ($imagenes as $imagen): ?>
        <img src="<?= $imagen->ruta ?>" alt="<?= $imagen->descripcion ?>">
    <?php endforeach; ?>
</div>

<h2>Próximos eventos</h2>
<ul>
    <?php foreach ($eventos as $evento): ?>
    <li><?= $evento->fecha ?> - <?= Html::link("eventos/ver/{$evento->id}", $evento->titulo) ?></li>
    <?php endforeach; ?>
</ul>
